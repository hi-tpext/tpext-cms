# tpext CMS 使用文档

本文档基于 `common/taglib/Cms.php`、`common/taglib/Table.php`、`functions.php` 及 `tpl/demo.html` 整理。

- 标签库解析类：[common/taglib/Cms.php](common/taglib/Cms.php)
- 表与标签映射：[common/taglib/Table.php](common/taglib/Table.php)
- 查询函数与助手函数：[functions.php](functions.php)
- 在线演示模板：[tpl/demo.html](tpl/demo.html)（后台模板管理中可生成演示页）

---

## 一、标签总览

系统内置 5 张表的映射标签，同时提供通用的 `list` / `get` / `parents` / `prev` / `next` 标签。

| 表 | 别名标签 | 主键 | 分类键(cid) | 默认排序 |
|---|---|---|---|---|
| cms_channel | channel | id | parent_id | sort asc,id asc |
| cms_content | content | id | channel_id | sort desc,publish_time desc,id desc |
| cms_banner | banner | id | position_id | sort asc,id asc |
| cms_position | position | id | - | sort asc,id asc |
| cms_tag | tag | id | - | sort asc,id asc |

### 标签形式

| 标签 | 类型 | 说明 |
|---|---|---|
| `xxx@list`（如 `content@list`） | 闭合标签 | 列表查询 |
| `xxx@get`（如 `content@get`） | 非闭合 | 单条查询 |
| `xxx@parents`（如 `channel@parents`） | 闭合标签 | 查询上级层级列表 |
| `xxx@prev` / `xxx@next` | 非闭合 | 上一篇/下一篇 |
| `list` / `get` / `parents` / `prev` / `next` | - | 通用标签，需指定 `table="表名"` |
| `show@vars` | 非闭合 | 调试：显示当前页面环境变量 |

通用标签的循环变量默认为 `item`，单条变量默认为 `data`；别名标签（如 `content@list`）默认使用别名作为变量名（`content`）。

---

## 二、列表标签

`content@list` / `channel@list` / ... / `list table="xxx"`

### 支持的属性

| 属性 | 说明 |
|---|---|
| `id` | 主键条件，见[条件表达式](#五条件表达式) |
| `cid` / `pid` | 分类键语法糖：`cid` 等同 `cid_key` 字段（如 `channel_id`、`parent_id`），`pid` 等同 `parent_id` |
| `where` | 查询条件，见[where 查询条件](#53-where-查询条件) |
| `order` | 排序，支持变量或方法，见[order](#六排序-order) |
| `num` | 限制输出条数，不产生分页链接 |
| `pagesize` | 每页条数，大于 0 时产生分页，支持变量或方法，见[分页](#七分页) |
| `fields` | 查询字段，默认 `*`。谨慎使用，有的字段在表间关联时需要 |
| `item` | 指定循环变量名，默认别名（`content`）或 `item` |
| `assign` | 把列表赋值给变量，可在标签外循环输出 |
| `cache` | 缓存，见[缓存](#八缓存) |
| `links` | 控制分页链接位置，见[分页](#七分页) |
| `simple` | 简单分页模式 |

### 示例

```html
<!-- 栏目id为3的文章 -->
{content@list cid="3"}
<div>栏目id：{$content.channel_id}|文章id：{$content.id}|<a href="{$content.url}">{$content.title}</a></div>
{/content@list}

<!-- 限制条数、排序、缓存 -->
{content@list cid="7" num="3" order="sort asc" item="hello" cache="index_news_7,3600"}
<div>栏目id：{$hello.channel_id}|文章id：{$hello.id}|排序：{$hello.sort}</div>
{/content@list}

<!-- 通用标签指定表 -->
{list table="cms_content" where="channel_id in(6,7,8)"}
<div>栏目id：{$item.channel_id}|文章id：{$item.id}</div>
{/list}

<!-- 赋值到变量，标签外输出 -->
{content@list where="title like '%经济%'" num="3" assign="mylist"}
{/content@list}
{volist name="mylist" id="content"}
<div>{$content.id}:{$content.title}</div>
{/volist}
```

未指定 `cid` 时，会自动尝试取当前页面的 `$channel_id`（或 `$channel_ids`）变量；未指定 `id` 时取 `$id`。

---

## 三、单条标签

`content@get` / `channel@get` / ... / `get table="xxx"`

### 支持的属性

| 属性 | 说明 |
|---|---|
| `id` | 主键条件 |
| `cid` | 分类键语法糖 |
| `where` | 查询条件 |
| `order` | 排序（find 取第一条），支持变量或方法 |
| `fields` | 查询字段，默认 `*` |
| `assign` | 赋值变量名，默认别名（`content`、`channel`）或 `data` |
| `cache` | 缓存 |

### 示例

```html
{content@get id="100"}
<div>栏目id：{$content.channel_id}|文章id：{$content.id}|<a href="{$content.url}">{$content.title}</a>|发布日期：{$content.date}</div>

{get table="cms_channel" id="6"}
<div>栏目id：{$data.id}|<a href="{$data.url}">{$data.name}</a></div>
```

---

## 四、上级列表 / 上一篇下一篇

### parents 上级列表

`channel@parents` / `parents table="xxx"`：查询当前节点的各级上级，支持 `id_key`、`pid_key`（默认 `id` / `parent_id`）、`assign`、`item`、`fields` 属性。

```html
{channel@parents id="5"}
<div>{$channel.name}</div>
{/channel@parents}
```

### prev / next 上一篇下一篇

`content@prev` / `content@next`：依据 `order` 的第一个字段与当前值比较。属性：`id`、`where`、`order`、`fields`、`assign`（默认 `prev` / `next`）、`cache`、`sort`（自定义当前值表达式，默认 `$content.排序字段`）。

> 注意：prev / next 的 `order` **不支持变量或方法**，因为编译期需要解析排序字段来构造比较条件，只支持字面量，如 `order="id desc"`。

```html
<a href="{$prev.url}">上一篇：{$prev.title}</a>
<a href="{$next.url}">下一篇：{$next.title}</a>
```

---

## 五、条件表达式

### 5.1 id / cid / pid

| 写法 | 说明 |
|---|---|
| `id="5"` | 等于 5 |
| `id="2,4,6"` | 相当于 `id in(2,4,6)` |
| `cid="gt 5"` | 大于 5 |
| `id="between 70 and 90"` | 区间 |
| `cid="$a"` | 使用变量，自动做安全过滤（sql_guard） |
| `cid=":intval($a)"` | 以 `:` 开头为函数调用写法 |

支持表达式：`gt` `eq` `lt` `egt` `elt` `neq` `!=` `>` `<` `=` `in` `not in` `like` `not like` `between` `not between`。

> 注意：`id` 只支持一个条件，不支持 `id="gt 3 and lt 10"` 这种多条件写法；多条件请使用 `where`。
> 尽量用 `gt/eq/lt` 等写法而不用尖括号 `> <`，部分编辑器会把尖括号识别为 html 标签。

### 5.2 where 语法糖与表达式

`where` 中会尝试把 `cid` 替换为真实分类字段（如 `channel_id`）、`pid` 替换为 `parent_id`，并把 `gt/eq/lt/egt/elt/neq/not in/not like/between` 等表达式转换为标准 SQL 运算符。

```html
{content@list where="id gt 30 and id elt 40"}
{/content@list}
```

> where 表达式较复杂，语法糖可能替换失败，建议 where 中使用真实字段名。

### 5.3 where 查询条件

#### 内嵌变量（参数化绑定，防注入）

```html
<!-- 标量 -->
{content@list where="channel_id=$cid" pagesize="10"}
<!-- LIKE，%$var% 自动拼接百分号 -->
{content@list where="title like %$kwd% or description like %$kwd%" pagesize="10"}
<!-- 函数参数 -->
{list table="cms_content" where="FIND_IN_SET($id,tags)" pagesize="10"}
```

`where` 中的 `$var`、`$arr['key']`、`$obj.prop` 会被提取为命名参数绑定到 `whereRaw`，不会拼接 SQL 字符串。

#### 整体是一个变量

`where` 的值整体是一个变量时，直接把变量值作为查询条件：

- 字符串：作为原始 sql 条件（**不支持** gt/eq 等语法糖，请写标准 SQL 运算符），自动经 `sql_guard` 过滤
- 数组：作为 `where()` 的数组查询条件，如 `['id'=>5]`、`[['status','=',1]]`（需在提前assign赋值）

```html
<!-- 模板中提前赋值 -->
{assign name="where" value="id > 30 and id <= 40" /}
{content@list where="$where"}

<!-- 全局函数 -->
{content@list where=":getPageFilter()"}

<!-- 静态方法 -->
{content@list where="\xxx\yyy::getPageFilter()"}
```

---

## 六、排序 order

`order` 支持字面量、变量、方法三种写法：

| 写法 | 编译结果 |
|---|---|
| `order="sort asc"` | 字面量直接使用 |
| `order="$order"` | 运行时取模板变量，经 `sql_guard` 过滤 |
| `order=":getOrder()"` | 调用全局函数 |
| `order="\xxx\yyy::getOrder()"` | 调用静态方法 |

```html
{assign name="order" value="sort asc" /}
{content@list order="$order"}
```

> 变量值必须是标准 SQL 排序字符串；未定义变量兜底为空字符串。

---

## 七、分页

| 属性 | 说明 |
|---|---|
| `pagesize` | 每页条数，大于 0 时产生分页链接，分页页码从 url 的 `page` 参数读取 |
| `num` | 只限制条数，**不产生**分页链接 |
| `links` | 值为 `0`/`false`/`n`/`no` 时，列表下方的分页链接不显示 |
| `simple` | 简单分页模式 |

`pagesize` 支持变量或方法（值自动转为整数）：

```html
{content@list pagesize="$pagesize"}
{content@list pagesize="getPagesize()"}
{content@list pagesize="\xxx\yyy::getPagesize()"}
```

完整分页示例（列表赋值后在指定位置输出分页链接）：

```html
{content@list id="between 70 and 90" pagesize="5" links="no" assign="mylist"}
{/content@list}

<div>{$__links_html__|raw}</div>

{volist name="mylist" id="content"}
<div>{$content.id}:{$content.title}</div>
{/volist}
```

> `links="no"` 只控制跟随列表位置的分页链接，`{$__links_html__|raw}` 输出不受影响。

---

## 八、缓存

`cache="缓存键,缓存秒数"`，如 `cache="index_news_7,3600"`（缓存 1 小时）。

```html
{content@list cid="7" num="3" cache="index_news_7,3600"}
```

不设置 `cache` 或缓存键为空时默认不缓存。

---

## 九、安全机制

1. **允许的表**：内置 5 张表默认允许；其他表需通过 `Table::addAllowTables(['products'])` 或后台配置允许。表名包含 `admin` 的一律拒绝。
2. **未允许的表**：标签整体替换为 `<!--数据表：xxx未允许使用标签-->`，标签体（包括其中的字段输出、嵌套标签）不会执行。
3. **变量过滤**：`id`/`cid` 的变量值、`order` 和 `where` 整体变量的字符串值都会经过 `sql_guard()` 过滤（去 html 标签、拦截 select/union 注入特征）。
4. **参数化绑定**：`where` 中内嵌的变量以绑定参数方式执行，不拼接 SQL。
5. **动态页参数**：动态解析页自动注入的 url 参数经过 `strip_tags()` 基本过滤；对明确类型的参数建议用 `:intval()` 转换：

url参数自动转换为变量如：`xxxx.html?a=1&b=2`

```html
{content@list cid=":intval($a)" num=":intval($b)"}
```

---

## 十、页面类型与自动注入

| 页面类型 | 模板目录 | 访问路径 | 说明 |
|---|---|---|---|
| 首页 | theme 根 | `/` 或 `/index.html` | 自由使用标签 |
| 栏目模板 | channel/ | `/c/[id].html` | 自动注入 `$channel`；`content@list` 的分页大小、排序自动注入栏目设置 |
| 详情模板 | content/ | `/d/[id].html` | 自动注入 `$content`，无需再使用 `content@get` |
| 动态解析页 | dynamic/ | `/e/xxx.html` | 自由发挥，自动注入 url 参数，不可静态化 |
| 单页 | theme 根 | `/xx.html` | 绑定文章后等同详情模板；未绑定文章等同动态页 |

- 栏目模板中：直接使用 `{$channel.name}` 输出栏目信息；`content@list` 一般无需额外参数（id、pagesize、order 都会注入后台设置），需要个性化时再按需加参数覆盖。
- 详情模板中：直接使用 `{$content.title}` 输出文章信息。
- 静态化：详情页全部可静态化；栏目页第一页可静态化（`/c/4.html`、`/c/c4-1.html`）；动态解析页不可静态化。

---

## 十一、扩展表与扩展标签

### 增加表标签映射

监听 `tpext_cms_get_tables` 事件调用 `Table::extend($tables)`（或在框架启动时调用）：

```php
tpext\cms\common\taglib\Table::extend([
    'products' => [
        'desc' =>'表描述', //如产品
        'tag_name' => 'products',   // 标签名，使用 products@list
        'id_key' => 'id',
        'cid_key' => 'cat_id',      // 分类键（语法糖 cid 对应的字段）
        'pid_key' => '',   //
        'default_order' => 'sort asc,id asc',
        'default_fields' => '*',
        'default_scope' => 'is_show=1', // 默认查询条件，自动附加
    ],
]);
```

### 允许其他表使用通用标签

```php
tpext\cms\common\taglib\Table::addAllowTables(['products', 'xxx']);
```

允许后即可使用：

```html
{list table="products" where="..."}
{products@list where="..."}
```

### 模型与扩展表

栏目信息的"附加表"中填写扩展表（如 `products`）后，该栏目页使用扩展表查询（`{list table="products" ...}` 或 `{products@list ...}`），详情页无差别。

---

## 十二、助手函数

模板中可直接使用的函数（[functions.php](functions.php)）：

| 函数 | 说明 |
|---|---|
| `more($str, $len, $more='...')` | 字符串截取：`{$content.title|more=36,'...更多'}` 或 `{:more($content.title,36,'...更多')}` |
| `channel_url($id)` | 栏目 url：`{:channel_url(3)}` |
| `content_url($id)` | 内容 url：`{:content_url(100)}` |
| `banner_url($id)` | 广告 url |
| `tag_url($id)` | 合集 url |
| `thumb($path, $width, $height)` | 缩略图：`{$banner.image|thumb=400,200}` |
| `url_filter($params)` | 生成当前页面url参数`?xxx=oo`，包含全部`get`参数，`$params`参数会覆盖/附加当前`get`参数 |


自定义助手函数：在 `app/common.php`（tp5 为 `application/common.php`）中定义即可在模板中使用。ThinkPHP/PHP 内置函数同样可用，如 `intval`、`str_replace` 等。

> **输出转义注意**：框架对变量输出自动追加 `htmlentities` 过滤，但仅限管道形式——`{$content.title|more=36}` 编译为 `htmlentities(more(...))`，过滤保留；而冒号函数形式 `{:more($content.title,36)}` 不走过滤器链，输出是**原始未转义**的。字段处理建议优先使用管道写法；必须使用冒号形式时请手动包裹：`{:htmlentities(more($content.title,36))}`。`|raw` 为显式跳过转义。

---

## 十三、调试

在页面任意位置使用 `{show@vars}` 显示当前页面环境变量（`$vars`），套标签时很有用：

```html
{show@vars}
```

---

## 十四、主题目录结构

```
theme/xxx/
├─channel/            [栏目模板]
│   ├─default.html    [默认栏目模板，请勿删除或改名]
│   └─....            [更多模板，绑定到栏目]
├─common/             [公共片段：头部、底部、布局等，用 include 引用]
├─content/            [详情模板]
│   ├─default.html    [默认详情模板，请勿删除或改名]
│   └─....
├─dynamic/            [动态解析模板，访问 /e/xxx]
├─static/             [静态资源：css/images/js，用 @static@ 引用]
├─index.html          [首页]
├─about.html          [单页，可绑定一篇文章]
└─....
```

- 静态资源引用：`@static@css/style.css` 或相对路径 `../static/css/style.css`
- 路径替换：`@site_home@`（首页路径）、`@static@`（静态资源路径）
- 页面路径个性化：栏目/文章的访问路径可在栏目编辑的【生成设置】中自定义，如 `/c/news.html`、`/d/news-[id].html`

---

## 附：标签属性速查

| 属性 | 适用标签 | 支持变量/方法 | 说明 |
|---|---|---|---|
| `table` | 通用标签 | - | 指定数据表 |
| `id` | 全部 | 是（`$var` / `:func()`） | 主键条件 |
| `cid` / `pid` | list/get/parents | 是 | 分类键语法糖 |
| `where` | 全部 | 是（整体或内嵌） | 查询条件，见 5.3 |
| `order` | list/get（prev/next 仅字面量） | 是 | 排序 |
| `num` | list | 是 | 条数限制，不分页 |
| `pagesize` | list | 是 | 分页大小 |
| `fields` | 全部 | - | 查询字段，默认 `*` |
| `item` | list/parents | - | 循环变量名 |
| `assign` | 全部 | - | 赋值变量名 |
| `cache` | 全部 | - | `键,秒数` |
| `links` | list | - | 分页链接开关 |
| `simple` | list | - | 简单分页 |
| `id_key` / `pid_key` | parents | - | 自定义主键/上级键 |
| `sort` | prev/next | - | 自定义当前值表达式 |
